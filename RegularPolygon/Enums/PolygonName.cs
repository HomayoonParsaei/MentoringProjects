
namespace RegularPolygon.Enums
{
    public enum PolygonName
    {
        Triangle = 3,       // (III)
        Quadrilateral = 4,  // (IV)
        Pentagon = 5,       // (V)
        Hexagon = 6,        // (VI)
        Heptagon = 7,       // (VII)
        Octagon = 8,        // (VIII)
        Nonagon = 9,        // (IX)
        Decagon = 10,       // (X)
        Hendecagon = 11,    // (XI)
        Dodecagon = 12      // (XII)
    }
}
